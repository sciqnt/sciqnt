# sq-finnhub — unit manifest
name: sq-finnhub
kind: source
risk_tier: read
status: proof
flavours:
  api:
    risk: official           # documented REST API, free tier
    dependencies: []         # stdlib only
schema_version: 0
capabilities:
  read:
    - get_news               # company news for a US-style symbol
  execute: []
inputs:
  - ticker
  - api_token                # BYO free key: keychain sq-finnhub/api_token or FINNHUB_API_KEY
dependencies: []             # stdlib only
caveats: >
  Free tier 60 calls/min. Keyless = inert (the news chain falls through
  to sq-news-rss). Company-news coverage is US-centric; EU venue tickers
  may return nothing — the chain handles it. News is CONTEXT only.
known_quirks: FINDINGS.md
license: MIT
