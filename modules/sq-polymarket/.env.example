# sq-polymarket — copy to `.env` (same folder) and fill in. The wallet address
# is PUBLIC (not a secret). For proxy wallets use the FUNDER address from your
# Polymarket profile (where positions + USDC live, not the signing EOA).
POLYMARKET_WALLET=
